apiVersion: '1'
id: 'credentials'
description: 'Bundle with extra credentials plugins'
version: '3'

parent: 'default'
jcasc:
  - 'jenkins.yaml'
plugins:
  - 'plugins.yaml'
catalog:
  - 'plugin-catalog.yaml'
